# Ignore everything by default — whitelist package files only
*

# Runtime state — never commit these
.loop-signal

# Package source
!src/
!src/**
!tests/
!tests/**
!assets/
!assets/**

# Task specs and shared docs (loop infrastructure)
!tasks/
!tasks/**
!ERROR_CONVENTIONS.md

# Package config
!package.json
!package-lock.json
!tsconfig.json
!tsup.config.ts
!vitest.config.ts
!vitest.setup.ts
!biome.json
!.gitignore
